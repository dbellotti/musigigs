class Gig < ActiveRecord::Base
  has_and_belongs_to_many :user
    
  def self.search(search)
    if search
      find(:all, :conditions => ['band_title LIKE ? or date Like #{}', "%#{search}%"])
    else
      find(:all)
    end
  end
end
