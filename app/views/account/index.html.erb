<h1> Welcome <%= @user.user_name %></h1>

It's <%= Time.now %><br />
There are <%= @total_users %> total users<br /><br />

Administrators will redirect to => /users <br />
Users will redirect to => musigig profile <br /><br />

<%= button_to 'Find Gigs', :controller => 'gigs', :action => 'search' %>
<%= button_to 'Logout', :action => 'logout' %>
